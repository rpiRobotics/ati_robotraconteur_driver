# ATI F/T Sensor RobotRaconteur Driver


## Install Dependencies
`python -m pip install -r requirements.txt`

## Usage
`python robotraconteur_ati_driver.py --sensor-ip=<Sensor IP>`
Sensor settings available in browser at `<Sensor IP>`